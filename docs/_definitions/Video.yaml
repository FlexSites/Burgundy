properties:
  id:
    type: string
  credits:
    type: string
    description: Noteworthy things about this entertainer
  description:
    type: string
    description: Bio or short summary
  social:
    type: object
    $ref: '#/definitions/Social'
  media:
    type: array
    items:
      type: object
      $ref: '#/definitions/Medium'
  verified:
    type: boolean
    description: Whether or not this entertainer has been verified by FlexSites
  events:
    type: array
    items:
      type: object
      $ref: '#/definitions/Event'

